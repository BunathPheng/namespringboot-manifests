---
# Source: helm for kubernetes/templates/ingress.yaml
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: namespringboot                  # Ingress name
  labels:
    app: namespringboot
spec:
  ingressClassName: nginx   # Optional ingressClass
  rules:
    - host: "namespringboot.smartops.space"
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: namespringboot               # Links to Service name dynamically
                port:
                  number: 8080  # Links to service port dynamically
